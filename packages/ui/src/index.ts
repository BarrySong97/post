/**
 * @purpose Expose the shared UI package entrypoint for workspace consumers.
 * @role    Package boundary for reusable style exports and future shared UI primitives.
 * @deps    @post/ui package exports.
 * @gotcha  Keep app-specific component behavior in apps/desktop unless it is genuinely shared.
 */

export {};
