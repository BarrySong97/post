/// <reference types="vite/client" />

/**
 * @purpose Load Vite client ambient types for the Electron renderer build.
 * @role    TypeScript environment shim for electron-vite web compilation.
 * @deps    vite/client type declarations.
 * @gotcha  Keep the triple-slash reference at the top so TypeScript treats it as a directive.
 */
